export var MAX_FILE_COUNT = 20;
export var dataTransferItemsToArray = function (items) {
    var result = [];
    for (var i = 0; i < items.length; i++) {
        if (items[i].kind === 'file') {
            result.push(items[i].getAsFile());
        }
    }
    return result;
};
//# sourceMappingURL=utils.js.map